spring:
  application:
    name: annotations-demo
  datasource:
    url: jdbc:mysql://localhost:3306/university?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Mysql@123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect